# Development-only overrides. Copy to `.env` only when changing defaults.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin-dev-only
MINIO_BUCKET=amp-development
MONGO_EXPRESS_USER=devadmin
MONGO_EXPRESS_PASSWORD=devadmin-only
REDIS_UI_USER=devadmin
REDIS_UI_PASSWORD=devadmin-only
