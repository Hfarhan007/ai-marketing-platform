import { Alert } from '@/shared/ui';
export function ConsentNotice() { return <Alert title="Operational aid, not legal advice">These frontend records do not establish regulatory compliance by themselves. Confirm requirements with qualified counsel.</Alert>; }
