import { Input, Select, Textarea } from '@/shared/ui';
import type { Agent } from '../types/agent.types';

export function AgentInstructions({ agent, onChange }: { agent: Agent; onChange: (agent: Agent) => void }) {
  const patch = (values: Partial<Agent>) => onChange({ ...agent, ...values });
  return <section className="grid gap-4"><div className="grid gap-4 sm:grid-cols-2"><Input label="Agent name" onChange={(event) => patch({ name: event.target.value })} value={agent.name} /><Input label="Avatar initials" maxLength={3} onChange={(event) => patch({ avatar: event.target.value.toUpperCase() })} value={agent.avatar} /><Select label="Status" onChange={(event) => patch({ status: event.target.value as Agent['status'] })} options={['active', 'draft', 'paused'].map((value) => ({ label: value[0]!.toUpperCase() + value.slice(1), value }))} value={agent.status} /><Select label="Language" onChange={(event) => patch({ language: event.target.value })} options={['English', 'Spanish', 'French', 'German', 'Arabic'].map((value) => ({ label: value, value }))} value={agent.language} /><Select label="Tone" onChange={(event) => patch({ tone: event.target.value as Agent['tone'] })} options={['professional', 'friendly', 'concise', 'empathetic'].map((value) => ({ label: value[0]!.toUpperCase() + value.slice(1), value }))} value={agent.tone} /><Input label="Business hours" onChange={(event) => patch({ businessHours: event.target.value })} value={agent.businessHours} /></div><Textarea label="Description" onChange={(event) => patch({ description: event.target.value })} rows={2} value={agent.description} /><Textarea label="System instructions" onChange={(event) => patch({ systemInstructions: event.target.value })} rows={8} value={agent.systemInstructions} /><Textarea label="Welcome message" onChange={(event) => patch({ welcomeMessage: event.target.value })} rows={3} value={agent.welcomeMessage} /><Textarea label="Fallback response" onChange={(event) => patch({ fallbackResponse: event.target.value })} rows={3} value={agent.fallbackResponse} /></section>;
}
