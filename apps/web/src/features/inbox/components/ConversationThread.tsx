import { Check, CheckCheck, FileText, Image } from 'lucide-react';
import { Avatar, Badge } from '@/shared/ui';
import { cn } from '@/shared/utils/cn';
import type { InboxContact, Message } from '../types/inbox.types';

export function ConversationThread({ contact, messages, typing }: { contact: InboxContact; messages: readonly Message[]; typing: boolean }) {
  return <div className="flex-1 overflow-y-auto bg-slate-50 px-4 py-5 dark:bg-slate-950 sm:px-6"><div className="mx-auto grid max-w-3xl gap-4">{messages.map((message) => message.kind === 'note' ? <article className="mx-auto max-w-xl rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-950 dark:border-amber-900 dark:bg-amber-950/50 dark:text-amber-100" key={message.id}><Badge className="mb-1" tone="warning">Internal note · {message.senderName}</Badge><p>{message.body}</p></article> : <article className={cn('flex max-w-[85%] items-end gap-2', message.sender === 'agent' && 'ml-auto flex-row-reverse')} key={message.id}>{message.sender === 'contact' ? <Avatar alt={contact.name} size="sm" /> : null}<div><div className={cn('rounded-2xl px-4 py-3 text-sm shadow-sm', message.sender === 'agent' ? 'rounded-br-md bg-indigo-600 text-white' : 'rounded-bl-md border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-900')}>{message.body}{message.attachments.length ? <div className="mt-3 grid gap-2">{message.attachments.map((attachment) => <button className={cn('flex items-center gap-2 rounded-lg border p-2 text-left text-xs', message.sender === 'agent' ? 'border-indigo-400 bg-indigo-500' : 'border-slate-200 bg-slate-50 dark:border-slate-700 dark:bg-slate-800')} key={attachment.id} type="button">{attachment.type === 'image' ? <Image size={16} /> : <FileText size={16} />}<span><strong className="block">{attachment.name}</strong><span className="opacity-70">{attachment.size}</span></span></button>)}</div> : null}</div><div className={cn('mt-1 flex items-center gap-1 text-[11px] text-slate-500', message.sender === 'agent' && 'justify-end')}><time>{new Intl.DateTimeFormat('en', { hour: 'numeric', minute: '2-digit' }).format(new Date(message.createdAt))}</time>{message.sender === 'agent' ? message.status === 'read' ? <CheckCheck aria-label="Read" className="text-indigo-500" size={13} /> : message.status === 'delivered' ? <CheckCheck aria-label="Delivered" size={13} /> : <Check aria-label={message.status ?? 'Sent'} size={13} /> : null}</div></div></article>)}{typing ? <div className="flex items-end gap-2"><Avatar alt={contact.name} size="sm" /><div aria-label={`${contact.name} is typing`} className="flex gap-1 rounded-2xl rounded-bl-md border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-900" role="status"><span className="size-1.5 animate-bounce rounded-full bg-slate-400" /><span className="size-1.5 animate-bounce rounded-full bg-slate-400 [animation-delay:120ms]" /><span className="size-1.5 animate-bounce rounded-full bg-slate-400 [animation-delay:240ms]" /></div></div> : null}</div></div>;
}
