import{Injectable}from'@nestjs/common';import type{WorkspaceRequestContext}from'../../../common/types/workspace-context.js';import{CrmCrudService}from'../../crm/crud.service.js';import{CrmEventService}from'../../crm/crm-event.service.js';import{CrmJobsService}from'../../crm/crm-jobs.service.js';import{SchedulingJobsService}from'../../scheduling/scheduling-jobs.service.js';import{assertTimeZone}from'../../scheduling/time.js';import{CreateTaskDto,UpdateTaskDto}from'../dto/task.dto.js';import{TasksRepository}from'../repositories/tasks.repository.js';import type{Task}from'../schemas/task.schema.js';
const map=(v:Task)=>({id:String(v._id),title:v.title,description:v.description,status:v.status,priority:v.priority,dueAt:v.dueAt,timezone:v.timezone,reminders:v.reminders,ownerId:v.ownerId?String(v.ownerId):null,contactId:v.contactId?String(v.contactId):null,companyId:v.companyId?String(v.companyId):null,dealId:v.dealId?String(v.dealId):null,checklist:v.checklist,parentTaskId:v.parentTaskId?String(v.parentTaskId):null,subtaskIds:v.subtaskIds.map(String),recurrence:v.recurrence,completionHistory:v.completionHistory,version:v.version,createdAt:v.createdAt,updatedAt:v.updatedAt});
@Injectable()export class TasksService extends CrmCrudService<Task,CreateTaskDto,UpdateTaskDto>{constructor(r:TasksRepository,e:CrmEventService,j:CrmJobsService,private readonly scheduling:SchedulingJobsService){super(r,e,j,'tasks',map)}protected override prepare(d:CreateTaskDto){assertTimeZone(d.timezone);return d}override async create(c:WorkspaceRequestContext,d:CreateTaskDto){const v=await super.create(c,d);if(d.recurrence&&d.dueAt)await this.scheduling.recurringTask(c.workspaceId,String((v as{id:string}).id),d.dueAt);return v}}
