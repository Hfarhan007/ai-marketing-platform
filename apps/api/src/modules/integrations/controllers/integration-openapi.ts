import{applyDecorators,type Type}from'@nestjs/common';import{ApiBadGatewayResponse,ApiBadRequestResponse,ApiBearerAuth,ApiForbiddenResponse,ApiHeader,ApiNotFoundResponse,ApiOperation,ApiResponse,ApiServiceUnavailableResponse,ApiTooManyRequestsResponse,ApiUnauthorizedResponse}from'@nestjs/swagger';
const error=(description:string)=>({description});
export function ApiWorkspaceIntegration(summary:string,permission:'integrations.read'|'integrations.manage',type:Type<unknown>,status=200){return applyDecorators(ApiOperation({summary,description:`Authentication required. Requires an active workspace and the \`${permission}\` permission.`}),ApiBearerAuth(),ApiHeader({name:'x-workspace-id',required:true,description:'Active workspace MongoDB ID'}),ApiResponse({status,type,description:'Successful response'}),ApiBadRequestResponse(error('Invalid request or provider configuration')),ApiUnauthorizedResponse(error('Authentication missing, expired provider credentials, or reauthorization required')),ApiForbiddenResponse(error(`Missing ${permission} or provider permission`)),ApiNotFoundResponse(error('Workspace-scoped integration or resource was not found')),ApiTooManyRequestsResponse(error('Application or provider rate limit reached')),ApiBadGatewayResponse(error('Provider returned an invalid or failed response')),ApiServiceUnavailableResponse(error('Provider is temporarily unavailable')))}
export function ApiPublicProviderEndpoint(summary:string,type:Type<unknown>,status=200){return applyDecorators(ApiOperation({summary,description:'Public only for provider delivery. Provider state or signature verification is mandatory; no bearer authentication or workspace header is accepted as proof.'}),ApiResponse({status,type}),ApiBadRequestResponse(error('Malformed provider payload')),ApiUnauthorizedResponse(error('OAuth state or webhook signature validation failed')),ApiNotFoundResponse(error('Integration connection was not found')),ApiTooManyRequestsResponse(error('Webhook rate limit reached')),ApiServiceUnavailableResponse(error('Provider processing is temporarily unavailable')))}
