import{describe,expect,it}from'vitest';import{boundedBackoff,retryAfterMs,transportFailure}from'./provider-error.js';
describe('provider reliability errors',()=>{it('honors delta-seconds and HTTP-date Retry-After values with a cap',()=>{expect(retryAfterMs('3',0)).toBe(3000);expect(retryAfterMs(new Date(5000).toUTCString(),0)).toBe(5000);expect(retryAfterMs('999',0)).toBe(60000)});it('bounds exponential backoff and never ignores a longer provider delay',()=>{expect(boundedBackoff(0)).toBe(500);expect(boundedBackoff(99)).toBe(30000);expect(boundedBackoff(1,5000)).toBe(5000)});it('classifies timeout, DNS, and reset failures without leaking low-level details',()=>{expect(transportFailure('meta',Object.assign(new Error('secret URL'),{cause:{code:'ENOTFOUND'}}))).toMatchObject({code:'META_DNS_ERROR',kind:'dns',retryable:true});expect(transportFailure('highlevel',Object.assign(new Error('socket'),{cause:{code:'ECONNRESET'}}))).toMatchObject({kind:'connection_reset'});expect(transportFailure('meta',Object.assign(new Error('wait'),{name:'TimeoutError'}))).toMatchObject({kind:'timeout'})})});
