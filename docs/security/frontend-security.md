# Frontend security

Treat all client state as user-visible. Sanitize untrusted rich HTML, avoid dynamic script execution, never persist secrets, and repeat every permission and validation rule in future backend services.
