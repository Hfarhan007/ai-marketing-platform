# Frontend deployment

Build with the pinned Node and pnpm versions. Run frozen installation, lint, type-check, tests, and build. Serve `apps/web/dist` behind HTTPS with SPA fallback and security headers.
