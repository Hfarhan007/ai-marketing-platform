# Accessibility guidelines

Use semantic controls, programmatic labels, keyboard navigation, visible focus indicators, live announcements for asynchronous results, and the app shell's skip link. Validate changes with ESLint and browser tests.
