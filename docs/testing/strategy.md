# Testing strategy

Vitest covers units and React Testing Library covers components. Playwright owns browser flows. New stateful utilities require unit tests; critical workspace journeys require desktop and mobile browser coverage.
