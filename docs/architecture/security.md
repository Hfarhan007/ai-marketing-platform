# Frontend security

Frontend permissions improve UX but do not create authorization. Browser storage is limited to non-sensitive preferences. CSP must be supplied by the hosting layer. No access tokens, secrets, or real credentials belong in the client.
