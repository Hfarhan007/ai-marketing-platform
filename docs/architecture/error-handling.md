# Error handling

`AppErrorBoundary` catches unexpected render failures and reports them through the provider-neutral error reporter. `RouteErrorBoundary` supports router failures. Detailed errors render only during development.
