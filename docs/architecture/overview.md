# Frontend architecture

The React/Vite client lives in `apps/web`. `app` composes providers and routes, `layouts` owns page shells, `shared` contains domain-neutral code, and `features` owns product behavior. Workspace packages provide reusable types, validation, configuration, UI, and SDK foundations.
