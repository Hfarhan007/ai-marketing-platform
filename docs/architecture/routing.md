# Routing

React Router defines public, guest, onboarding, and workspace trees. Feature pages use dynamic imports. Workspace routes pass through authentication and optional permission guards before rendering inside `AppLayout`.
